[package]
name = "axnet1"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "AXNET/1 wire protocol shared by the StarryOS client and the FreeRTOS RTOS server"

[features]
default = ["alloc"]
alloc = []

[dependencies]
