[package]
name = "rtos-tester"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Host-side validator for the FreeRTOS AXNET/1 RTOS server in QEMU"

[[bin]]
name = "rtos-tester"
path = "src/main.rs"

[dependencies]
axnet1 = { workspace = true }
starry-client = { path = "../starry-client" }
